<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cine">
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".views.activity.LoginActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activity.MainActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".views.activity.SignUpOne"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".views.activity.EditProfile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".views.activity.SingUpFinal"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".views.activity.MyWallActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".views.activity.LanguageActivity"
            android:theme="@style/AppTheme" />
      <!--  <activity android:name=".views.activity.AdsActivity"
            android:theme="@style/AppTheme"></activity>-->
        <activity android:name=".views.activity.ImageViewer"
            android:theme="@style/AppTheme"/>
        <activity android:name=".views.activity.LanguageDialogActivity"
            android:label="Choose language"
            android:theme="@android:style/Theme.Holo.Light.Dialog"/>
    </application>

</manifest>